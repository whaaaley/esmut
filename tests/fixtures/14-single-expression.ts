// One statement and one site, which is the smallest input that still produces a mutation.
// A bare node type addresses everything here, so any selector longer than that is the ladder overshooting.

export const answer = 42
